<%=form_for @invite, :url => invites_path do |f| %>
    <%= f.hidden_field :story_id, :value => @invite.story_id %>
    <%= f.label :email %>
    <%= f.email_field :email %>
    <%= f.submit 'Send' %>
<% end %>